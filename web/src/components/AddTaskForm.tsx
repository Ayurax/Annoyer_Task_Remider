/**
 * Form for adding tasks from a laptop browser.
 *
 * TODO: Capture title, notes, due date/time, nag interval, and whether the task is personal or group-owned.
 * TODO: Writes should include the localStorage-backed anonymous device UUID.
 */
export function AddTaskForm() {
  return null;
}
