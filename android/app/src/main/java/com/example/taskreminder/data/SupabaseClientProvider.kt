package com.example.taskreminder.data

/**
 * Creates and owns the Supabase client wrapper for Android.
 *
 * TODO: Read Supabase URL and anon key from build config or secure local configuration.
 * TODO: Use the anonymous device UUID as the app-level identity for all writes.
 */
class SupabaseClientProvider
