package com.example.taskreminder.data

/**
 * Owns the anonymous device identity for Android.
 *
 * TODO: Generate a random UUID on first launch and persist it in DataStore.
 * TODO: Expose the stored UUID to Room sync, Supabase writes, FCM registration, and group joins.
 */
class DeviceIdentityStore
