package com.example.taskreminder

import android.os.Bundle
import androidx.activity.ComponentActivity

/**
 * Android app entry point.
 *
 * TODO: Set up the Compose navigation graph for task list, add task, settings, and group join screens.
 * TODO: Initialize anonymous device identity from DataStore before syncing with Supabase.
 */
class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // TODO: Render Compose app shell.
    }
}
