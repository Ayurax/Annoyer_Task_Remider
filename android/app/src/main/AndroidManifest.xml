<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- TODO: Add notification, exact alarm, and full-screen intent permissions once behavior is implemented. -->

    <application
        android:allowBackup="true"
        android:label="Task Reminder"
        android:theme="@style/Theme.TaskReminder">

        <service
            android:name=".notifications.TaskReminderFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
